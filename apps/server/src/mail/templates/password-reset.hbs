<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reset Your Password - Elevate</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      background-color: #F5F3F0;
      color: #1B3A57;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #FFFFFF;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }
    .header {
      background: linear-gradient(135deg, #1B3A57 0%, #2B4A6A 100%);
      padding: 32px;
      text-align: center;
    }
    .logo {
      color: #F6B756;
      font-size: 28px;
      font-weight: 700;
      letter-spacing: -1px;
      margin: 0;
    }
    .content {
      padding: 48px 32px;
    }
    .title {
      font-size: 24px;
      font-weight: 700;
      color: #1B3A57;
      margin: 0 0 16px 0;
    }
    .text {
      font-size: 16px;
      line-height: 1.6;
      color: #4B5563;
      margin: 0 0 24px 0;
    }
    .button {
      display: inline-block;
      background-color: #F6B756;
      color: #1B3A57;
      padding: 14px 32px;
      text-decoration: none;
      border-radius: 6px;
      font-weight: 600;
      font-size: 16px;
      text-align: center;
      transition: background-color 0.2s;
    }
    .button:hover {
      background-color: #E5A845;
    }
    .link {
      color: #1B3A57;
      word-break: break-all;
      font-size: 14px;
      line-height: 1.6;
    }
    .footer {
      background-color: #F9FAFB;
      padding: 24px 32px;
      text-align: center;
      border-top: 1px solid #E5E7EB;
    }
    .footer-text {
      font-size: 14px;
      color: #6B7280;
      margin: 0 0 8px 0;
    }
    .footer-link {
      color: #2B8A8A;
      text-decoration: none;
    }
    .divider {
      height: 1px;
      background-color: #E5E7EB;
      margin: 32px 0;
    }
    .security-note {
      background-color: #FEF3C7;
      border-left: 4px solid #F6B756;
      padding: 16px;
      margin: 24px 0;
      border-radius: 4px;
    }
    .security-note-text {
      font-size: 14px;
      color: #92400E;
      margin: 0;
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <h1 class="logo">ELEVATE</h1>
    </div>

    <!-- Content -->
    <div class="content">
      <h2 class="title">Reset Your Password</h2>
      <p class="text">
        We received a request to reset your password for your Elevate account. 
        Click the button below to create a new password.
      </p>

      <!-- CTA Button -->
      <div style="text-align: center; margin: 32px 0;">
        <a href="{{url}}" class="button">Reset Password</a>
      </div>

      <!-- Alternative Link -->
      <div class="divider"></div>
      <p class="text" style="font-size: 14px; margin-bottom: 8px;">
        If the button doesn't work, copy and paste this link into your browser:
      </p>
      <p class="link">{{url}}</p>

      <!-- Security Note -->
      <div class="security-note">
        <p class="security-note-text">
          <strong>Security Note:</strong> This link will expire in 24 hours. 
          If you didn't request a password reset, please ignore this email or contact support.
        </p>
      </div>

      <p class="text" style="font-size: 14px; margin-top: 32px;">
        If you have any questions, feel free to reach out to us at 
        <a href="mailto:{{supportEmail}}" style="color: #2B8A8A;">{{supportEmail}}</a>.
      </p>
    </div>

    <!-- Footer -->
    <div class="footer">
      <p class="footer-text">
        This email was sent by <strong>Elevate</strong>
      </p>
      <p class="footer-text">
        <a href="{{supportUrl}}" class="footer-link">Need Help?</a> | 
        <a href="{{baseUrl}}" class="footer-link">Visit Elevate</a>
      </p>
    </div>
  </div>
</body>
</html>

